# [2026-06-02] 일일 매매 리포트

> ⚠️ **분석 대상 DB = `kis_trader_real`** (실전 모드 운영 중). 모의 `kis_trader`는 당일 사이클 0 = 비가동(뉴스 수집기만 가동).
> **실전 첫 종일 정상 가동일**. 06-01의 사이클 폭주·API 한도 소진 문제는 **해소**되어, 종일 ~10초 간격으로 안정 가동했다.

## 요약
| 항목 | 값 |
|------|-----|
| 총 매매 | **1건** (매수 1 / 매도 0) |
| 실현 수익률 | N/A (매도 0건) · 보유 1종목 평가 ≈ **-1.5%** |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 52종목 (전환율 표기 9.6% / 5종목 converted — 단, 실거래는 1종목뿐, 아래 정합성 주석) |
| 시그널 | ENSEMBLE 2,245건 / 실행 1건 (act_rate 0.0%, avg_conf 0.542) — **전건 단일 종목(대한해운)** |
| 에러(ERROR) | 0건 (WARNING 1건 = 수동 킬스위치) |
| 사이클 시작/완료/스킵 | 2281 / 2281 / 0 (09:00~15:20, ~10초 간격) |
| API 한도 도달 | **0건** (06-01 대비 해소) |

**핵심 한 줄**: 실전 첫 종일 가동일로 **시스템은 완전 정상**(사이클 2281, API 한도·ERROR 0, 안정 10초 간격)이었다. 그러나 저장된 시그널이 **전부 대한해운(005880) 단일 종목**이었고, 그 1종목을 09:04에 1회 진입(BUY 24주 @2,080)한 뒤로는 추가 진입 대상이 형성되지 않아 **실거래 1건**에 그쳤다. 0~1건은 장애가 아니라 게이트·종목풀의 정상 귀결이다.

## 일중 타임라인 (시스템 거동)
| 시각(KST) | 사건 | 근거 |
|-----------|------|------|
| 08:30 | 장 시작 전 작업 정상 실행 (cron mon-fri) | autotrader.log `"장 시작 전 작업 시작/완료"` |
| 08:55 | 장중 매매 작업 등록: 09:00~15:20, **간격 10.0초** | log `"장중 매매 작업 등록"` |
| 09:00 | 첫 사이클 시작 (모니터링 0종목으로 출발, 이후 스크리닝 편입) | system_metrics MIN(CYCLE_START) |
| 09:04:51 | **유일 체결** — 대한해운(005880) BUY 24주 @2,080 (ENSEMBLE) | trades 정본 |
| 13:35:45 | **수동 킬스위치 활성 (.trading_halt) — 매매 동결** WARNING 1건 | event_logs WARNING (현재 `.trading_halt` 파일 부재 = 해제됨) |
| 15:20:00 | 마지막 사이클 | system_metrics MAX(CYCLE_START) |

## 체결 내역
| 시각 | 종목 | 구분 | 가격 | 수량 | 금액 | 사유 |
|------|------|------|------|------|------|------|
| 09:04:51 | 대한해운(005880) | BUY | 2,080 | 24 | 49,920 | ENSEMBLE |

- 매도 0건 → 실현 손익·승률 N/A.
- **보유 현황(portfolios)**: 대한해운 24주, 평단 2,086.67 / 현재가 2,055 / peak 2,130 → 평가손익 ≈ **-1.5%** (미실현). 손절선·익절선 미도달로 보유 유지 중.

## 전략 시그널 분석
- **signal_type 분포**: ENSEMBLE 2,245건, **전부 대한해운(005880) 단일 종목** (06-01의 삼성전자 단일 집중과 동일 패턴).
- **action_taken**: 1건 (09:04 BUY). act_rate 0.04%(표기 0.0%).
- **confidence**: avg **0.542** — `STRATEGY_MIN_CONFIDENCE` 임계 통과. **저신뢰가 미실행 사유가 아님**.
- **미실행 2,244건 해석**: 09:04에 대한해운을 이미 진입(보유)한 뒤로는, 동일 종목 추가 진입이 `MAX_DAILY_TRADES_PER_STOCK`·보유 중복 차단에 막혀 **정당 스킵**된 것으로 본다. (단, `signal_value.skip_reason`이 오늘은 미기록 → 스킵 사유 라벨링 보강 여지. 아래 개선 포인트)
- **타 종목 시그널 0건**: 52종목을 스크리닝했으나 대한해운 외에는 ENSEMBLE 신뢰도가 `STRATEGY_MIN_CONFIDENCE` 미만이어서 **저장조차 안 됨**(signals 생존편향). 즉 "발굴은 많으나 진입 후보는 사실상 1종목".

## 스크리닝 효율
- 발굴 **52종목**, `converted_to_trade=true` **5종목**(003280 흥아해운·005880 대한해운·018880 한온시스템·034220 LG디스플레이·090710 휴림로봇) → 표기 전환율 **9.6%**.
- ⚠️ **데이터 정합성 경고(06-01에 이어 2일째)**: 오늘 실거래는 **대한해운 1종목**뿐인데 converted=true는 **5종목**. 흥아해운·한온시스템·LG디스플레이·휴림로봇은 trades·signals가 전혀 없는데 converted=true → **`converted_to_trade` 플래그가 정본과 불일치**. LG디스플레이·휴림로봇은 06-01에도 converted=true로 중복 등장 → 플래그가 *실거래 시점*이 아니라 *발굴/의도 시점* 또는 누적으로 세팅되는 것으로 의심. **전환율 지표 신뢰 불가 상태 지속**.

## 에러/경고
| 시각 | 심각도 | 내용 | 추정 원인/메모 |
|------|--------|------|----------------|
| 13:35:45 | WARNING | 수동 킬스위치 활성 — 매매 동결 (.trading_halt) | 운영자 수동 동결 추정. 현재 `.trading_halt` 부재 = 해제됨. 오후 진입 차단에 일부 기여 가능(단정 불가, WARNING 1건뿐) |

- `event_logs` **ERROR 0건**. API_LIMIT 0건. 서킷 트립 0건. → 안정성 양호.

## 시스템 안정성
- **사이클**: 2281 시작 / 2281 완료 (가동률 100%, 미완료·스킵 0). 09:00~15:20 전 구간 ~10초 간격 → **06-01 폭주(~2초/사이클) 완전 해소**.
- **API 한도**: 일일 한도 도달 0회 (06-01의 ~5만 소진 대비 정상화). v0.8.3 핫픽스(0종목 폴백 → 간격 하한 우회 버그 제거) 효과가 첫 종일 가동에서 **검증됨**.
- **버전 정합성 회복**: `src/__version__.py` = `pyproject.toml` = **0.8.7** (06-01에 지적된 0.8.2 vs 0.8.3 불일치 해소). 이후 v0.8.5~0.8.7은 스크리닝 소스단 필터 보강(관리/정리매매/ETF 제외) 커밋.

## 이전 제안서 효과 검증
- 최근 14일 `implementation_logs`: 실전 DB는 06-01 부트스트랩 → 누적 이력 희소. **v0.8.3**(사이클 폭주/API 한도) 효과는 오늘자로 **검증 완료**(API 한도 0, 안정 10초). v0.8.5~0.8.7(스크리닝 소스 필터)의 효과는 발굴 종목 품질·전환율 패턴이 더 쌓인 뒤 평가 예정.

## 개선 포인트 (제안서는 생성하지 않음 — 사유 명기)
1. **[룰 A 미충족]** ENSEMBLE 7일 act_rate 0.0%(<50%)이나 avg_confidence **0.532 ≥ 0.3** → 룰 A의 AND 조건 불충족. **낮은 실행률의 원인은 신뢰도가 아니라 "진입 후보 종목풀이 단일"**이라는 구조적 문제이므로 MIN_CONFIDENCE 상향은 오처방. 제안서 생성 안 함.
2. **[룰 B 미충족]** 스크리닝 전환율 06-01=31.3% / 06-02=9.6% → 3일 연속 <10% 아님. + 실전 데이터 2일뿐. 제안서 생성 안 함.
3. **[룰 D 미충족]** 오늘 api_limit_hits=0 → 06-01의 1건은 v0.8.3로 이미 해소. 2일 연속 아님. 제안서 생성 안 함.
4. **[룰 C·E 미충족]** ERROR 0건 / 매도일수 **0**(<20) → 룰 C·E 비해당.
5. **[관측/추적 — 데이터 더 필요]**
   - `screening_results.converted_to_trade` 정합성 불량 **2일 연속 확인**(5 converted vs 실거래 1). 전환율 지표가 의사결정 근거로 쓰이는 만큼, 플래그 세팅 시점 로직 점검이 필요하나 **코드 수정 제안은 패턴 1~2일 더 확인 후**. (메모리 "스크리닝 필터 우회" 선결 과제와 연관)
   - 저장 시그널의 단일 종목 집중(2,245건 전부 대한해운). signals 생존편향으로 "진입 후보 다양성"이 지표에 안 잡힘 → 진입 후보 폭 자체가 좁은지(스크리닝 산출 vs 모니터링 편입 괴리, 메모리 항목) 추적.
   - 시그널 `skip_reason` 미기록 → 미실행 사유 가시성 보강 여지(관측성).
6. **[데이터 축적 필요]** 실전 거래일 2일·매도 0건. 시그널/리스크 파라미터(룰 A·E) 판단은 **최소 5~20 거래일 축적 후 재분석**.

---
*생성: 일간 루틴 (자동) · 분석 DB: kis_trader_real · 기준시각: 2026-06-02 16:30 KST*
