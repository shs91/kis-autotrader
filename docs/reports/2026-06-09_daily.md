# [2026-06-09] 일일 매매 리포트

> ⚠️ **분석 대상 DB = `kis_trader_real`** (실전 모드). DB 검증 가드 통과(`current_database()=kis_trader_real`). 모의 `kis_trader`는 분석 대상 아님.
> ✅ **6/8 리포트 P0 가설 정정**: "`signals` 6/3 이후 적재 중단 = 신호 파이프라인 회귀(P0)" 가설은 **반증되었다**. 오늘 `system_metrics`에 **SIGNAL_SUMMARY 2,252 / SIGNAL_SKIP 9,008 / EVAL_TARGETS 2,281**이 기록되어 신호 파이프라인은 **정상 가동** 중이며, `signals` 테이블 0건은 **전 신호가 HOLD(`skip_reason=hold_action`)라 미적재된 생존편향**임이 데이터로 확정된다.

## 요약
| 항목 | 값 |
|------|-----|
| 거래일 성격 | 정상 영업일 (화) — 종일 가동 09:00~15:20 |
| 총 매매 | **0건** (매수 0 / 매도 0) |
| 실현 수익률 | N/A (매수·매도 0건) |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 42종목 (converted_to_trade=4 — **후보 승격 의미, 실체결 아님**, 아래 §스크리닝) |
| 시그널(`signals` 테이블) | **0건** (생존편향 — 종일 전 신호 HOLD, conf 0.0) |
| 신호 파이프라인(`system_metrics`) | SIGNAL_SUMMARY 2,252 / SIGNAL_SKIP 9,008 / EVAL_TARGETS 2,281 — **정상 가동** |
| 에러(ERROR) | 0건 (`event_logs` 오늘 전 레벨 0행 — 관측성 공백 지속) |
| 사이클 시작/완료/스킵 | **2,281 / 2,281 / 0** (가동률 100%) |
| API 한도 도달 | 0건 |
| 버전 | `src/__version__.py` = `pyproject.toml` = **0.9.1** |

**핵심 한 줄**: 시스템은 09:00~15:20 종일 풀가동(2,281/2,281, API·서킷·에러 0)했고 신호 파이프라인도 정상 작동했다. 그럼에도 매매 0건인 이유는 **① 스크리닝이 후보를 09:04 단 1회만 산출(이후 76회 candidate_count=0)했고, ② 그 4종목이 종일 stale 평가대상으로 잔류했으며, ③ 4종목 전부 매 사이클 HOLD(일중 max_confidence=0.0)였고, ④ 보유 포지션이 종일 0이라 매도 자체가 불가**했기 때문이다. 이는 신호 적재 회귀가 아니라 **만성 구조 문제(발굴 1회성 + stale 잔류 + 급등주-전략 불일치)**이며, 별도 추적 중인 매매 튜닝 로드맵(Phase 0~4) 영역이다.

## 일중 거동 (요약)
- 09:00~15:20 ~10초 간격 2,281 사이클 풀가동. API 한도·서킷 트립·metric ERROR 0건.
- EVAL_TARGETS 분포: **positions=0 / screening=4** 가 2,252 사이클(개장 직후 29 사이클은 screening=0). 즉 종일 보유 0, 평가대상은 동일 4종목 고정.

## 체결 내역
당일 체결 **0건**.

> 보유 정본은 KIS 잔고 API 기준(DB `portfolios`는 후행 사본). EVAL_TARGETS의 positions=0이 종일 유지된 점으로 보아 현재 보유 포지션은 비어 있을 가능성이 높다(최근 체결은 6/5 09:10 대한해운 SELL로 직전 보유분 청산).

## 전략 시그널 분석
- **`signals` 테이블 오늘 0건** — 그러나 이는 신호 미생성이 아니다. `system_metrics` 기준 신호 파이프라인은 종일 작동:
  - SIGNAL_SUMMARY 2,252건: 매 사이클 `evaluated=4, hold_count=4, buy_count=0, sell_count=0, max_confidence=0.0`.
  - SIGNAL_SKIP 9,008건: **전량 `skip_reason=hold_action`**(signal_type=HOLD, confidence=0.0).
  - 일중 집계: `day_max_confidence=0.0`, `total_buy_signals=0`, `total_sell_signals=0`.
- 즉 4개 평가종목이 4개 전략(이동평균 3/20·RSI14·MACD·볼린저 20/2) 모두에서 HOLD를 받아 앙상블이 HOLD(conf 0.0)를 산출 → `_record_signal_to_db`가 HOLD/저신뢰를 미적재 → `signals` 0건. **6/3 이후 0건은 적재 버그가 아니라 생존편향**임이 재확인된다.
- ⚠️ 구조적 불일치: 평가 4종목은 모두 스크리닝 급등주(대한광통신 +13.39%·빛과전자 +13.73%·쏠리드 +6.93%·한온시스템 +4.08%)인데, 이들의 RSI(39~45 중립)·%B(0.12~0.21)·MACD(음수)는 평균회귀/추세 전략의 진입 조건과 어긋난다. **모멘텀 스크리닝이 공급하는 종목을 추세/평균회귀 앙상블이 매수하지 않는 미스매치**가 HOLD 지배의 직접 원인이다.
- 7일 롤링(쿼리 10): ENSEMBLE 4,531건 / act_rate 0.0% / avg_confidence 0.591 — **전량 6/3(임시공휴일 stale) 단일일 데이터**이며 6/4 이후 적재 0. 이 수치로 오늘을 판단하지 않는다.

## 스크리닝 효율
- 발굴 **42종목**, `converted_to_trade=true` **4종목**(대한광통신 010170·한온시스템 018880·빛과전자 069540·쏠리드 050890, 전부 09:04분) → 표기 전환율 9.5%.
- 📌 **converted_to_trade ↔ trades "모순" 해명**: 오늘 실체결 0인데 converted=4인 것은 데이터 버그가 아니다. SCREENING_CANDIDATE 기준 candidate_count=4가 나온 사이클은 **09:04 단 1회**뿐이고, EVAL_TARGETS의 종일 평가대상 4종목과 converted 4종목이 **정확히 일치**한다. 즉 `converted_to_trade`는 **스크리닝→평가후보 승격**을 의미하며 실체결과 무관하다. "전환율"은 체결률이 아니라 후보 승격률 지표다(6/8 리포트의 "정합성 경고/마킹 의심"은 정의 차이로 설명됨 → **마킹 버그 아님**).
- ⚠️ **발굴 1회성 + stale 잔류**: SCREENING_CANDIDATE 분포 = `candidate_count=4`가 1 사이클, **`candidate_count=0`이 76 사이클**(매번 ranked_total=30). 개장 직후 1회만 후보를 산출하고 이후 종일 신규 후보 0이며, 그 4종목이 EVAL_TARGETS에 종일 고정(`_screened_codes` stale 잔류). 이는 알려진 만성 패턴이다.
- 최근 전환율(쿼리 11): 6/3 16.7% / 6/5 12.5% / 6/8 6.8% / 6/9 9.5% → **3일 연속 <10% 아님**(6/5=12.5%) → 룰 B 미발동. (게다가 위 정의상 이 지표는 체결률이 아니므로 판정 근거로서 신뢰도 낮음.)
- SCREENING_RISK_EXCLUDED: 종목 001740이 매 스크리닝 사이클 리스크 게이트로 제외(count=1) — 리스크 게이트는 정상 작동.

## 에러/경고 · 관측성
| 항목 | 값 | 메모 |
|------|-----|------|
| `event_logs` 오늘(전 레벨) | **0행** | INFO/WARNING/ERROR 모두 0 |
| `system_metrics` ERROR(오늘) | 0건 | metric_errors 0 |
| API_LIMIT / RESTART | 0 / 0 | 가동 안정 |

- 🚨 **관측성 공백 지속**: 실전 `event_logs`가 오늘도 0행이라 일간 루틴의 에러 진단(쿼리 7)이 무력화된 상태가 이어진다. 단, 신호/스킵/차단 사유는 `system_metrics`(SIGNAL_SKIP·SCREENING_RISK_EXCLUDED·EVAL_TARGETS)에 풍부히 기록되어 **이번 진단은 그 채널로 충분히 수행**되었다.
- 📌 `event_logs` ERROR 미적재는 이미 제안서 `docs/proposals/2026-06-06_event-logs-error-integrity.md`(상태: **ready**, 미구현)가 근본원인(`engine.py`의 `log_error` 미호출)과 함께 다루고 있다 → **중복 제안 생성하지 않음**.

## 시스템 안정성
- 사이클 2,281 시작 / 2,281 완료(가동률 100%), API 한도·서킷 트립·metric ERROR 0건 → 순수 가동 안정성 양호.
- 가동 윈도우 09:00~15:20(정상 장 시간). 버전 0.9.1(`__version__.py` = `pyproject.toml` 정합).

## 이전 제안서 효과 검증
- 최근 구현(implementation_logs, 14일):
  - 6/8: **harness git_clean — docs 산출물 untracked 제외**(v0.9.1, 자동 구현 교착 해소) → 오늘 본 리포트 산출이 git_clean을 막지 않음(효과 정상).
  - 6/8: **장 마감 매매 진단 알림(C)** — 결산 직후 매매 사유/게이트 차단 가시화. 오늘처럼 매매 0건일 때 차단 사유를 운영자에게 알리는 채널이며, 본 리포트의 SIGNAL_SKIP/HOLD 진단과 정합.
  - 6/3: **holidays.json에 2026-06-03 임시공휴일 추가** — 오늘(영업일)은 직접 검증 대상 아님.
- `2026-06-06_event-logs-error-integrity.md`는 **아직 미구현(ready)** → auto-implement 처리 대기.

## 개선 포인트
1. **[구조적 · 매매 튜닝 로드맵에서 추적 중 — 제안서 미생성]** 오늘 매매 0건의 근본 원인은 신호/적재 장애가 아니라 **① 스크리닝 후보 1회성 산출(09:04 외 76 사이클 candidate_count=0) + ② stale 종목 종일 잔류(`_screened_codes`) + ③ 모멘텀 스크리닝 ↔ 평균회귀/추세 앙상블 미스매치(전 종목 HOLD)**의 결합이다. 이는 스크리닝·후보관리·전략 매칭 등 **실자금 매매 코어 로직 변경**을 요하며 BRIDGE_SPEC의 파라미터 자동 튜닝 안전범위를 벗어난다. 또한 이미 **매매 튜닝 로드맵 Phase 0~4**(커밋 `1fbe738`)가 이 영역을 계획하고 있으므로 **중복 제안서를 생성하지 않는다.** 다음 분석/사람 검토 우선 항목:
   - 스크리닝 후보가 개장 직후 1회만 채워지는 원인(스크리닝 주기 vs `_screened_codes` 갱신/만료 로직).
   - 급등주 위주 스크리닝과 평균회귀/추세 전략의 진입조건 불일치 완화(모멘텀 전략 비중 or 스크리닝 기준 정렬).
2. **[정정 · 진단 정합성]** 6/8 리포트의 P0 "`signals` 파이프라인 회귀" 가설은 SIGNAL_SUMMARY/SIGNAL_SKIP 존재로 **반증**. `signals` 0건 = 생존편향(전 신호 HOLD)으로 확정. 향후 신호 진단은 `signals` 테이블이 아니라 `system_metrics`(SIGNAL_SUMMARY/SIGNAL_SKIP)를 1차 소스로 삼을 것.
3. **[관측성 · 기존 제안서로 커버]** `event_logs` 공백 → `2026-06-06_event-logs-error-integrity.md`(ready)가 ERROR 적재 복구를 다룸. auto-implement 대기.

## 룰 엔진 판정 요약
| 룰 | 조건 | 판정 |
|----|------|------|
| A 시그널 전환율 저하 | act_rate<50% **AND** avg_conf<0.3 | 미발동 (7일 avg_conf 0.591 ≥ 0.3; 오늘 `signals` 0행) |
| B 스크리닝 전환율 악화 | 3일 연속 <10% | 미발동 (6/5=12.5%로 연속 깨짐; 지표 자체가 체결률 아님) |
| C 에러 반복 | 동일 category 3+ & 전일 동일 | 미발동 (오늘 ERROR 0, `event_logs` 공백) |
| D API 한도 도달 | api_limit_hits>0 | 미발동 (0) |
| E 리스크 파라미터 | sell_days≥20 | 미발동 (28일 sell_days=1) |

→ **정량 룰 A~E 전부 미발동 → 자동 제안서 0건.** 룰이 포착하지 못하는 구조적 매매 0건 원인은 §개선포인트 1에 기록(실자금 코어 변경·로드맵 추적 영역 → 자동 구현 금지, 조사/로드맵 우선).
