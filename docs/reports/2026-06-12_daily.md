# [2026-06-12] 일일 매매 리포트

> ⚠️ **분석 대상 DB = `kis_trader_real`** (실전 모드). DB 검증 가드 통과(`current_database()=kis_trader_real`). 모의 `kis_trader`는 분석 대상 아님.
> 🔎 **6/11 대비 변화**: 어제는 `signals` 테이블이 **0건**(순수 전 종목 HOLD)이었으나, 오늘은 **후성(093370) 단일 종목의 미보유 SELL 노이즈 2,251건이 재발**했다. 6/10 대원강업(2,252건) → 6/11 소멸 → 6/12 후성으로, **노이즈를 일으키는 종목만 바뀌며 같은 거동이 재발**하는 패턴이 확인됐다. 매매 0건·보유 0·발굴 1회성·HOLD 미스매치라는 만성 구조는 6/9~6/11과 동일하다.

## 요약
| 항목 | 값 |
|------|-----|
| 거래일 성격 | 정상 영업일 (금) — 종일 가동 09:00~15:20 |
| 총 매매 | **0건** (매수 0 / 매도 0) |
| 실현 수익률 | N/A (매수·매도 0건) |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 46종목 (converted_to_trade=5 — **후보 승격 의미, 실체결 아님**, 아래 §스크리닝) |
| 시그널(`signals` 테이블) | **2,251건** (후성 093370 단일 종목, ENSEMBLE, action_taken=false, conf 0.336 고정 — **미보유 SELL 노이즈**) |
| 신호 파이프라인(`system_metrics`) | SIGNAL_SUMMARY 2,251 / SIGNAL_SKIP 9,004 / EVAL_TARGETS 2,280 — **정상 가동(buy 0 / hold 4 / sell 1)** |
| 에러(ERROR) | 0건 (`event_logs`·`system_metrics` 모두 0 — **진짜 무에러**) |
| 사이클 시작/완료/스킵 | **2,280 / 2,280 / 0** (가동률 100%) |
| API 한도 도달 | 0건 |
| 버전 | `src/__version__.py` = `pyproject.toml` = **0.9.2** |

**핵심 한 줄**: 시스템은 09:00~15:20 종일 풀가동(2,280/2,280, API·서킷·에러 0)했고 신호 파이프라인도 정상이었다. 매매 0건의 원인은 6/9~6/11과 동일한 **① 보유 포지션 종일 0(EVAL_TARGETS positions=0 → 매도 자체 불가) + ② 스크리닝 후보 09:01 단 1회 산출(이후 candidate_count=0) + ③ 그 5종목 stale 잔류 + ④ 4종목 HOLD·1종목(후성) 미보유 SELL(실행 불가)로 신규 진입 동력 0(buy_count 종일 0, max_confidence 0.336)**의 결합이다. 어제와의 차이는 **후성 1종목이 미보유 SELL 신호를 종일 산출해 `signals`에 노이즈 2,251건을 다시 채운 점**이며, 6/10 대원강업 패턴의 재발이다. 모두 **실자금 매매 코어 로직** 영역으로 매매 튜닝 로드맵(Phase 0~4, 커밋 `1fbe738`)에서 추적 중이다.

## 일중 거동 (요약)
- 09:00:00~15:20:00 ~10초 간격 2,280 사이클 풀가동. API 한도·서킷 트립·metric ERROR 0건.
- 신호 평가는 **09:04:51부터** 시작(첫 후보 산출 09:01:12 이후). 초반 29 사이클(09:00:00~09:04:50)은 평가대상 0이라 SIGNAL_SUMMARY 미기록 → `2,280 - 29 = 2,251`이 SIGNAL_SUMMARY/`signals` 건수와 정합.
- EVAL_TARGETS 분포: **positions=0 / screening=5 / watchlist=0** (평가가 일어난 2,251 사이클 동일). 즉 종일 보유 0, 평가대상은 스크리닝 5종목 고정.
- 평가대상 5종목(고정): 대한광통신(010170)·대원전선(006340)·아주IB투자(027360)·한온시스템(018880)·후성(093370).
  - 6/11 평가대상(케이뱅크·흥아해운·랩지노믹스·SK네트웍스 4종목)과 **전종목 교체**. 후성은 6/10 평가대상에 있었다가 6/11 이탈, 오늘 재진입.

## 체결 내역
당일 체결 **0건**.

> 보유 정본은 KIS 잔고 API 기준(DB `portfolios`는 후행 사본). EVAL_TARGETS의 positions=0이 종일 유지된 점으로 보아 현재 보유 포지션은 비어 있다. 보유 0 → 매도 신호가 나와도 매도 자체가 불가하며, 오늘 유일한 SELL(후성)도 미보유라 `sell_without_position`으로 실행 차단됐다.

## 전략 시그널 분석
- **`signals` 테이블 오늘 2,251건 = 후성(093370) 단일 종목**, 전부 `ENSEMBLE / action_taken=false / confidence 0.336(avg=max 동일, 고정값)`. 정본 SIGNAL_SUMMARY의 `sell_count=1, screening_sell=1, max_confidence=0.3357`과 정합 → 이 적재분은 **미보유 스크리닝 후보(후성)에 대한 SELL 신호**이며, 미보유이므로 실행되지 않았다(`action_taken=false`).
- `system_metrics` 기준 신호 파이프라인은 종일 정상 가동:
  - SIGNAL_SUMMARY 2,251건(최종 사이클): `evaluated=5, hold_count=4, sell_count=1, buy_count=0, screening_buy=0, max_confidence=0.3357`.
  - SIGNAL_SKIP 9,004건: HOLD 4종목 × 2,251 사이클 = 9,004 (전량 `hold_action` skip). 4종목(대한광통신·대원전선·아주IB투자·한온시스템) 4개 전략(MA·RSI·MACD·볼린저) 투표 모두 HOLD, confidence 0.0.
  - 일중 집계: `sum_buy=0`, 신규 진입 신호 0.
- ⚠️ **buy_count 종일 0**: 매수 신호가 단 한 건도 없었다. 스크리닝 5종목 중 4종목 HOLD·1종목 미보유 SELL → 신규 진입 동력 부재가 매매 0건의 직접 원인이며, 6/9~6/11의 "전 종목 HOLD/미보유 SELL"과 본질적으로 같다(모멘텀 스크리닝 ↔ 평균회귀/추세 앙상블 미스매치). 후성의 SELL conf 0.336도 진입/청산 임계에 못 미치는 약신호다.
- ⚠️ **미보유 SELL 적재 노이즈 재발(6/10 대원강업 → 6/12 후성)**: 어제(6/11)는 후보 구성상 노이즈가 자연 소멸했으나, 오늘 후성이 후보로 들어오면서 동일 거동이 재발했다. 미보유 후보에 SELL을 산출해 `signals`에 적재(action_taken=false)하는 거동 자체가 미수정이라는 점이 재확인된다(아래 §개선포인트 2).
- 7일 롤링(쿼리 10): ENSEMBLE 4,503건 / act_rate 0.0% / avg_confidence 0.384 — 사실상 **6/10(대원강업 2,252건)+6/12(후성 2,251건) 두 단일종목 미보유 SELL 노이즈의 합**이다. 이 수치는 "매수 기회"가 아니므로 룰 A 판정에서 의미 있는 시그널로 해석하지 않는다.

## 스크리닝 효율
- 발굴 **46종목**, `converted_to_trade=true` **5종목** → 표기 전환율 10.9%.
  - 5종목(09:01:12, cycle 233 단일 산출): 대한광통신(010170, rank5, +9.37%)·아주IB투자(027360, rank6, +11.3%)·한온시스템(018880, rank8, +7.92%)·대원전선(006340, rank11, +7.1%)·후성(093370, rank12, +7.55%).
- 📌 **converted_to_trade ↔ trades "모순" 해명(6/9~6/11과 동일)**: 오늘 실체결 0인데 converted=5인 것은 데이터 버그가 아니다. converted 5종목이 EVAL_TARGETS 종일 평가대상 5종목과 **정확히 일치**한다. 즉 `converted_to_trade`는 **스크리닝→평가후보 승격**을 의미하며 실체결과 무관하다(전환율 = 체결률이 아니라 후보 승격률).
- ⚠️ **발굴 1회성 + stale 잔류(만성 패턴 지속)**: SCREENING_CANDIDATE는 09:01:12(cycle 233)에 후보를 산출한 뒤 이후 사이클은 `candidate_count=0`(ranked_total=30 유지, 후보 승격 0). 개장 직후 1회만 후보를 채우고 그 5종목이 EVAL_TARGETS에 종일 고정(`_screened_codes` stale 잔류). 6/9~6/11과 동일한 알려진 만성 패턴.
- 최근 전환율(쿼리 11): 6/8 6.8% / 6/9 9.5% / 6/10 10.9% / 6/11 7.0% / 6/12 10.9% → **3일 연속 <10% 아님**(6/10=6/12=10.9%) → 룰 B 미발동. (지표 자체가 체결률이 아니므로 판정 근거 신뢰도 낮음.)

## 에러/경고 · 관측성
| 항목 | 값 | 메모 |
|------|-----|------|
| `event_logs` 오늘 ERROR | **0건** | v0.9.2(6/9) 적재 정합 픽스 반영, 오늘은 진짜 무에러 |
| `system_metrics` ERROR(오늘) | 0건 | metric_errors 0 |
| API_LIMIT / RESTART | 0 / 0 | 가동 안정 |
| 전일(6/11) ERROR | 0건 | 룰 C 비교용 — 연속 에러 없음 |

- ✅ **관측성**: `event_logs` ERROR 0행은 v0.9.2(`50bba8f`, "종목 처리 ERROR를 event_logs에도 적재", 6/9 implemented) 이후 "적재 누락"이 아니라 **실제로 에러가 0건**이었음을 의미한다(`system_metrics` metric_errors=0과 정합).

## 시스템 안정성
- 사이클 2,280 시작 / 2,280 완료(가동률 100%), API 한도·서킷 트립·metric ERROR 0건 → 순수 가동 안정성 양호.
- 가동 윈도우 09:00:00~15:20:00(정상 장 시간). 버전 0.9.2(`__version__.py` = `pyproject.toml` 정합).

## 이전 제안서 효과 검증
- 최근 구현(implementation_logs, 14일):
  - **6/9: `event_logs` 적재 정합 — 종목 처리 ERROR를 event_logs에도 기록**(v0.9.2) → 오늘 `event_logs` ERROR 0행이 "공백"이 아닌 "무에러"임을 보장. **효과 정상.**
  - 6/8: **장 마감 매매 진단 알림(C)** — 오늘처럼 매매 0건일 때 게이트 차단/HOLD 사유를 운영자에게 알리는 채널. 본 리포트의 buy_count=0 / 4종목 HOLD·1종목 미보유 SELL 진단과 정합.
  - 6/8: **harness git_clean — docs 산출물 untracked 제외**(v0.9.1) → 본 리포트 산출이 git_clean을 막지 않음(효과 정상).
  - 6/3: **holidays.json에 2026-06-03 임시공휴일 추가** — 오늘(영업일)은 직접 검증 대상 아님.

## 개선 포인트
1. **[구조적 · 매매 튜닝 로드맵에서 추적 중 — 제안서 미생성]** 매매 0건의 근본 원인은 **① 보유 0(매도 불가) + ② 스크리닝 후보 1회성 산출(09:01 외 candidate_count=0) + ③ stale 종목 종일 잔류(`_screened_codes`) + ④ 모멘텀 스크리닝 ↔ 평균회귀/추세 앙상블 미스매치(buy_count 종일 0, max_confidence 0.336으로 진입 임계 미달)**의 결합이다. 이는 스크리닝·후보관리·전략 매칭 등 **실자금 매매 코어 로직 변경**을 요하며 BRIDGE_SPEC 파라미터 자동 튜닝 안전범위를 벗어난다. **매매 튜닝 로드맵 Phase 0~4**(커밋 `1fbe738`)가 이 영역을 계획하므로 **중복 제안서를 생성하지 않는다.** 우선 조사 항목(6/9~6/11과 동일):
   - 스크리닝 후보가 개장 직후 1회만 채워지는 원인(스크리닝 주기 vs `_screened_codes` 갱신/만료 로직).
   - 급등주 위주 스크리닝과 평균회귀/추세 전략의 진입조건 불일치 완화(max_confidence가 종일 진입 임계 근처도 못 가는 점).
2. **[관측 노이즈 · 재발 확인 — 6/10 §개선포인트 1 / 6/11 §개선포인트 2 유지]** 오늘 후성 미보유 SELL 노이즈 2,251건은 6/10 대원강업(2,252건) 거동의 재발이다. 어제 소멸은 근본 수정이 아니라 종목 구성 변화에 따른 자연 소멸이었음이 이번 재발로 확정됐다. 미보유 스크리닝 후보에 SELL을 산출하고 `sell_without_position`으로 실행 차단되면서도 `signals`에 적재(action_taken=false)하는 거동은 신호 메트릭(act_rate/avg_confidence 7일 롤링)을 지속 오염시킨다. 수정 대상이 `_record_signal_to_db`(engine.py)/앙상블 산출 로직으로 **실자금 매매 코어 인접**이므로 자동 제안서 미생성, 사람 검토/로드맵 항목으로 유지. (조사 우선순위는 6/11 대비 ↑ — 재발 확정.)
3. **[관측성 · 해소 확인]** `event_logs` ERROR 공백은 v0.9.2로 수정 완료. 오늘 0건은 무에러로 확정. 추가 제안 불요.

## 룰 엔진 판정 요약
| 룰 | 조건 | 판정 |
|----|------|------|
| A 시그널 전환율 저하 | act_rate<50% **AND** avg_conf<0.3 | **미발동** (7일 act_rate 0%지만 avg_conf 0.384 ≥ 0.3; 게다가 4,503건은 6/10·6/12 단일종목 미보유 SELL 노이즈로 매수 기회 의미 없음) |
| B 스크리닝 전환율 악화 | 3일 연속 <10% | **미발동** (6/10=10.9%, 6/12=10.9%로 연속 깨짐; 지표 자체가 체결률 아님) |
| C 에러 반복 | 동일 category 3+ & 전일 동일 | **미발동** (오늘 ERROR 0, 전일 ERROR 0) |
| D API 한도 도달 | api_limit_hits>0 | **미발동** (0) |
| E 리스크 파라미터 | sell_days≥20 | **미발동** (28일 sell_days=1) |

→ **정량 룰 A~E 전부 미발동 → 자동 제안서 0건.** 룰이 포착하지 못하는 구조적 매매 0건 원인(보유 0 + 발굴 1회성 + HOLD/미보유 SELL 미스매치)과 미보유 SELL 노이즈 재발은 §개선포인트에 기록(실자금 코어 변경·로드맵 추적 영역 → 자동 구현 금지, 조사/로드맵 우선).
