# [2026-06-08] 일일 매매 리포트

> ⚠️ **분석 대상 DB = `kis_trader_real`** (실전 모드). DB 검증 가드 통과(`current_database()=kis_trader_real`). 모의 `kis_trader`는 분석 대상 아님.
> 🚨 **핵심: 정상 영업일·종일 풀가동(2,281 사이클)인데 `signals`·`trades` 모두 0건.** `signals` 테이블은 **2026-06-03 15:20 이후 단 한 건도 적재되지 않았다**(6/5·6/8 가동에도 0). 6/3(휴장일 stale)과 달리 **오늘은 영업일이라 "신호 파이프라인 회귀"가 강하게 의심된다 → P0 조사 항목**.

## 요약
| 항목 | 값 |
|------|-----|
| 거래일 성격 | 정상 영업일 (월) — 종일 가동 |
| 총 매매 | **0건** (매수 0 / 매도 0) |
| 실현 수익률 | N/A (매수·매도 0건) |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 73종목 (converted 5 / 표기 전환율 6.8% — **trades 0과 모순, 아래 주석**) |
| 시그널 | **0건** (🚨 6/3 15:20 이후 적재 완전 중단 — 저장 6,928행이 모두 6/3 이전) |
| 에러(ERROR) | 0건 (`event_logs` 오늘 전 레벨 0행 — 관측성 공백 지속) |
| 사이클 시작/완료/스킵 | **2,281 / 2,281 / 0** (가동률 100%) |
| API 한도 도달 | 0건 |
| 버전 | `src/__version__.py` = `pyproject.toml` = **0.8.8** |

**핵심 한 줄**: 시스템 가동 안정성(사이클 2,281/2,281, API·에러·서킷 0)은 양호하나, **신호 생성/적재가 6/4 이후 작동하지 않아 매매 파이프라인이 사실상 무력화**된 상태다. 가동은 정상으로 보이지만 신호가 0이면 진입 후보가 구조적으로 0이 되어 매매가 일어날 수 없다. 실전 실자금 계좌에서 P0 조사가 필요하다.

## 일중 거동 (요약)
- 09~15시 종일 ~10초 간격 사이클 풀가동(시간당 ~360, 15시 121). API 한도·서킷 트립·metric ERROR 0건.
- 그러나 이 2,281 사이클 동안 `signals` 적재 0, `trades` 0. 직전 영업일(6/2·6/3)에는 수천 건의 신호가 적재됐던 것과 대비된다.

## 체결 내역
당일 체결 **0건**.

> 실전 매도 이력 참고: 가장 최근 체결은 **2026-06-05 09:10 대한해운(005880) SELL 24주 @2,010 (STOP_LOSS, -3.67%)**. 이로써 직전 보유분(6/2 매수 대한해운)이 청산되어 현재 보유종목이 사실상 비었을 가능성이 있다(보유 정본은 KIS 잔고 API 기준 — DB portfolios는 후행 사본).

## 전략 시그널 분석
- 🚨 **오늘 시그널 0건.** `signals` 테이블 총 6,928행의 **최신 `detected_at`/`created_at`이 모두 2026-06-03 15:20**이다. 즉 6/4·6/5·6/8 가동에도 신호가 한 건도 기록되지 않았다.
- 최근 신호 적재 일별: **6/2 = 2,245건 / 6/3 = 4,531건 / 6/4·6/5·6/8 = 0건**.
- 7일 누적(쿼리 10): ENSEMBLE 6,776건, **act_rate 0.0%**(6,776건 중 acted 1건), avg_confidence 0.575. → 수치 전량이 6/2·6/3분이며, **act_rate 0%는 "신호는 떴으나 전부 미실행"**이었던 6/2·6/3의 구조적 교착(보유종목=BUY불가 / 급등주=SELL불가, 6/3 리포트 §전략 참조)을 반영한다.
- ⚠️ **avg_confidence 0.575 ≥ 0.3** → 저신뢰 미저장(생존편향)만으로는 "오늘 0건"을 설명할 수 없다. 직전일까지 신호가 풍부했다가 급감(수천→0)한 점은 저신뢰 컷이 아니라 **신호 생성 또는 DB 적재 경로의 회귀**를 가리킨다.

## 스크리닝 효율
- 발굴 **73종목**(최근 최다), `converted_to_trade=true` **5종목**(흥아해운·대한광통신·아주IB투자·에이치엠넥스·휴림로봇) → 표기 전환율 6.8%.
- ⚠️ **데이터 정합성 경고(6/1·6/2·6/3에 이어 4일째)**: 오늘 실거래는 **0건**인데 converted=true가 **5종목**(대부분 09:04~09:14, 흥아해운만 14:39). `converted_to_trade` 플래그가 실거래 정본과 불일치 지속 → **전환율 지표 신뢰 불가**. 매일 정확히 5종목이 converted로 마킹되는 패턴(6/2·6/3·6/5·6/8 모두 converted=5)은 구조적 마킹 로직 의심.
- 최근 전환율(쿼리 11): 6/2 9.6% / 6/3 16.7% / 6/5 12.5% / 6/8 6.8% → **3일 연속 <10% 아님** (룰 B 미발동).

## 에러/경고 · 관측성
| 항목 | 값 | 메모 |
|------|-----|------|
| `event_logs` 오늘(전 레벨) | **0행** | INFO/WARNING/ERROR 모두 0 |
| `event_logs` 최근 일별 | 6/2 INFO 11·WARN 1 / 6/5 INFO 1 / 그 외 0 | 실전 이벤트 적재가 극히 희소 |
| `system_metrics` ERROR(오늘) | 0건 | 6/5에는 ERROR 2건 있었음(종목 처리 실패) |
| API_LIMIT / metric_errors / RESTART | 0 / 0 / 0 | system_metrics 기준 가동 안정 |

- 🚨 **관측성 공백 지속**: 실전 `event_logs`가 거의 기록되지 않아(오늘 0행), 신호·차단·스킵 사유를 DB로 추적할 수 없다. 일간 루틴의 에러 진단(쿼리 7)이 실전에서 무력화된 상태가 이어진다.
- 📌 이 중 **종목 처리 ERROR의 event_logs 미적재**는 이미 제안서 `docs/proposals/2026-06-06_event-logs-error-integrity.md`(상태: **ready**, 미구현)가 근본원인(`engine.py`가 `log_error` 미호출)과 함께 다루고 있다 → **중복 제안 생성하지 않음**. 단, 이 제안서는 ERROR 적재만 복구하며, 아래 §개선포인트 1의 **`signals` 미적재는 별개 미커버 이슈**다.

## 시스템 안정성
- 사이클 2,281 시작 / 2,281 완료(가동률 100%), API 한도·서킷 트립·metric ERROR 0건 → **순수 가동 안정성 양호**.
- 6/4 = 0 사이클(비가동), 6/5 = 207 사이클(오전 부분 가동, ERROR 2건) 후 6/8 풀가동으로 회복. 이 6/4~6/5 중단·부분가동 구간이 신호 적재 회귀의 시점과 겹친다(상관 가능성, 인과 미확정).
- 버전: 0.8.8 (`__version__.py` = `pyproject.toml` 정합).

## 이전 제안서 효과 검증
- 최근 구현(implementation_logs): **v0.8.8 — `holidays.json` 2026-06-03 선거 임시공휴일 추가**(6/3 17:18 구현). 오늘(6/8)은 영업일이라 휴장일 게이팅의 직접 검증 대상이 아님(다음 휴장일에 검증).
- `2026-06-06_event-logs-error-integrity.md`는 **아직 미구현(ready)** → auto-implement 처리 대기. 효과 검증은 구현·다음 에러 발생 후로 보류.

## 개선 포인트
1. **[P0 · 제안서 미생성 — 코드 원인 규명 선행 필요]** 🚨 **`signals` 6/3 15:20 이후 완전 중단.** 영업일(6/8) 풀가동 2,281 사이클에도 신호 0건이며, 이것이 매매 0건의 근본 원인으로 의심된다. 데이터로 현상은 확정(쿼리 4·10 + C1/C2: max detected_at=6/3 15:20)되나 **코드 레벨 원인(신호 생성 단계 vs DB 적재 단계, real DB 세션/핸들러 회귀 여부)이 미규명**이다. 추측 기반 자동 코드 변경은 실전 매매에 고위험이므로 **ready 제안서를 생성하지 않는다.** 다음 분석/사람 조사에서 우선 규명할 것:
   - `src/engine.py`의 신호 생성→`signals` 적재 경로가 6/4 이후 호출되는지(예외 swallow 여부).
   - `event_logs` 공백과 **동일 근본원인(real DB 세션/적재 큐 장애)** 인지 — 그렇다면 6/6 제안서(ERROR import 보강)만으로는 불충분.
   - 6/4 비가동·6/5 부분가동 시점의 재시작/배포가 적재 경로를 깨뜨렸는지.
2. **[P1 · 기존 제안서로 일부 커버]** `event_logs` 관측성 공백 → `2026-06-06_event-logs-error-integrity.md`(ready)가 ERROR 적재를 복구. `signals` 미적재(개선포인트 1)와 근본원인 공유 여부를 조사 시 함께 확인.
3. **[제안서 미생성 — 진단 정합성]** `converted_to_trade` ↔ `trades` 4일 연속 불일치(매일 converted=5, 실거래 0~1). 전환율 지표가 실거래를 반영하지 못하므로 룰 B(스크리닝 전환율) 판정 신뢰도 저하. 마킹 로직 점검을 별도 조사 항목으로 남긴다.

## 룰 엔진 판정 요약
| 룰 | 조건 | 판정 |
|----|------|------|
| A 시그널 전환율 저하 | act_rate<50% **AND** avg_conf<0.3 | 미발동 (avg_conf 0.575 ≥ 0.3) |
| B 스크리닝 전환율 악화 | 3일 연속 <10% | 미발동 (6/8만 6.8%, 6/3·6/5 ≥10%) |
| C 에러 반복 | 동일 category 3+ & 전일 동일 | 미발동 (오늘 ERROR 0, event_logs 공백) |
| D API 한도 도달 | api_limit_hits>0 | 미발동 (0) |
| E 리스크 파라미터 | sell_days≥20 | 미발동 (28일 sell_days=1) |

→ **정량 룰 A~E 전부 미발동 → 자동 제안서 0건.** 단, 룰이 포착하지 못하는 **`signals` 적재 완전 중단(P0)**을 개선포인트 1에 기록했다(원인 미규명·실전 실자금 사안으로 자동 구현 금지, 조사 우선).
