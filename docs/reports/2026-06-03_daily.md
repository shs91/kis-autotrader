# [2026-06-03] 일일 매매 리포트

> ⚠️ **분석 대상 DB = `kis_trader_real`** (실전 모드 운영 중). 모의 `kis_trader`는 당일 `CYCLE_START` 0 = 비가동(뉴스 수집기만 가동).
> 🚨 **핵심: 2026-06-03은 제9회 전국동시지방선거 임시공휴일 → KRX 휴장일이다. 그럼에도 시스템이 2,281 사이클을 정상 가동했다.** `holidays.json`에 2026-06-03이 누락되어 휴장일 게이팅이 작동하지 않았다.
> 따라서 오늘의 시세·스크리닝·시그널 데이터는 **직전 영업일(06-02) 기준 stale 값일 가능성이 높아**, 본 리포트는 파라미터 튜닝 근거로 사용하지 않는다(공통규칙: 추측 금지).

## 요약
| 항목 | 값 |
|------|-----|
| 거래일 성격 | **휴장일(제9회 전국동시지방선거)** — 매매 불가 |
| 총 매매 | **0건** (매수 0 / 매도 0) — 휴장일이므로 정상 |
| 실현 수익률 | N/A (매도 0건) |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 30종목 (converted 5 / 표기 전환율 16.7% — **stale·정합성 의심, 아래 주석**) |
| 시그널 | ENSEMBLE 4,531건 / 실행 0건 (act_rate 0.0%, avg_conf 0.591) |
| 에러(ERROR) | 0건 (`event_logs` 자체가 06-02 07:18 이후 미기록 — 관측성 공백, 아래) |
| 사이클 시작/완료/스킵 | **2,281 / 2,281 / 0** (휴장일임에도 종일 가동) |
| API 한도 도달 | 0건 |

**핵심 한 줄**: 오늘은 KRX 휴장일(지방선거)인데 `holidays.json` 누락으로 시스템이 **휴장일에 2,281 사이클을 가동**했다. 매매 0건은 휴장일이므로 정상이나, 휴장일에 API를 소비하고 stale 데이터로 4,531건의 무의미 시그널을 생성한 것은 **게이팅 결함**이다. 추가로 06-02부터 이어진 (1) 단일종목(대한해운) 교착, (2) `event_logs` 미기록 관측성 공백이 지속된다.

## 일중 거동 (요약)
- 09:00~15:29 구간 종일 ~10초 간격 사이클(2,281회). 휴장일이라 KIS 시세 API는 직전 영업일(06-02) 종가 기반 값을 회신했을 것으로 추정.
- 오늘 저장 시그널 4,531건 = **BUY 2,280건(전부 005880 대한해운, skip_reason 없음) + SELL 2,251건(전부 스크리닝 급등주, `skip_reason="sell_without_position"`)**.
- 실행 0건. (휴장일 + 구조적 교착 복합)

## 체결 내역
당일 체결 **0건** (휴장일).

> 참고: 실전 전체 거래 이력은 06-02 09:04 대한해운(005880) BUY 24주 @2,080 **단 1건**뿐이며, 이후 추가 체결 없음(보유 유지 추정).

## 전략 시그널 분석
- **BUY 시그널 2,280건이 전량 단일 종목(대한해운 005880)**, avg_conf 0.552. 이미 06-02에 진입·보유 중인 종목이라 `MAX_DAILY_TRADES_PER_STOCK`·보유중복 차단으로 추가 진입 불가 → 진입 후보가 사실상 **0종목**.
- **SELL 시그널 2,251건은 전량 스크리닝된 급등주**(LG헬로비전 +10.84%, 아남전자 +10.47% 등). 앙상블이 과매수로 판정해 SELL을 내지만, 해당 종목 보유분이 없어 `skip_reason="sell_without_position"`으로 정당 스킵.
- confidence avg **0.591**(임계 0.1 충분 통과) → 저신뢰가 미실행 사유가 **아님**. 미실행은 (휴장일) + (종목풀 구조: 보유종목=BUY불가 / 급등주=SELL불가)의 결과.
- ✅ **개선 확인**: 06-02 리포트에서 지적한 `signal_value.skip_reason` 미기록이 **SELL 측은 기록되기 시작**(`sell_without_position`). 단, BUY 측 미실행 사유(보유중복/일일한도)는 여전히 라벨 미기록.

## 스크리닝 효율
- 발굴 30종목, `converted_to_trade=true` 5종목(LG헬로비전·LG디스플레이·이브이첨단소재·아남전자·SKAI) → 표기 전환율 16.7%.
- ⚠️ **데이터 정합성 경고(06-01·06-02에 이어 3일째)**: 오늘 실거래는 **0건**인데 converted=true가 **5종목**. `converted_to_trade` 플래그가 실거래 정본과 불일치 지속 → **전환율 지표 신뢰 불가**. 게다가 휴장일 stale 데이터 → 더욱 무의미.
- 최근 3일 전환율: 06-01 31.3% / 06-02 9.6% / 06-03 16.7% → **3일 연속 <10% 아님** (룰 B 미발동).

## 에러/경고 · 관측성
| 항목 | 값 | 메모 |
|------|-----|------|
| `event_logs` ERROR(오늘) | 0건 | — |
| `event_logs` 전체 행수 | **64행, 마지막 06-02 07:18** | 🚨 06-02 장중·06-03 전체 이벤트가 전혀 기록되지 않음 |
| API_LIMIT / metric_errors / RESTART | 0 / 0 / 0 | system_metrics 기준 안정 |

- 🚨 **관측성 공백**: 실전 DB `event_logs`가 06-02 07:18 이후로 멈췄다(총 64행). 06-02의 실체결 1건·06-03의 2,281 사이클이 있었음에도 이벤트 미기록 → **차단/스킵/경고 사유를 DB로 추적 불가**. 일간 루틴의 에러·차단 진단(쿼리 7)이 실전에서 사실상 무력화된 상태.

## 시스템 안정성
- 사이클 2,281 시작 / 2,281 완료(가동률 100%), API 한도·서킷 트립·metric ERROR 0건 → **순수 가동 안정성은 양호**.
- 그러나 **휴장일에 가동된 것 자체가 결함**: API 호출·연산 낭비 + stale 시세로 무의미 시그널 4,531건 생성.
- 버전: `src/__version__.py` = `pyproject.toml` = **0.8.7** (정합).

## 이전 제안서 효과 검증
- 최근 구현(implementation_logs, virtual DB 기준): v0.8.7 헬스체크 리포트 수정 / v0.8.6 스크리닝 소스단 필터 / v0.8.5 ETF·ETN 필터.
- 스크리닝 소스단 필터(v0.8.5~0.8.6)에도 불구하고 오늘 converted 종목이 급등주(LG헬로비전 +10.84% 등) 위주인 점은, **휴장일 stale 데이터** 때문일 수 있어 효과 판정을 보류한다(영업일 데이터로 재검증 필요).

## 개선 포인트 (제안서화)
1. **[제안서 생성 → ready]** `holidays.json`에 **2026-06-03(제9회 전국동시지방선거 임시공휴일) 추가**. 향후 선거일·임시공휴일 누락 방지 메모 포함. → `docs/proposals/2026-06-03_holidays-add-election-day.md`
2. **[제안서 미생성 — 사람/Cowork 검토 권고]** 단일종목 교착 구조(보유종목=BUY불가 / 스크리닝 급등주=SELL불가 → 신규 진입 후보 0). 룰 A~E 미발동(confidence 정상 0.59, 전환율 3일연속<10% 아님)이며, **오늘 데이터가 휴장일 stale이라 파라미터 튜닝 근거로 부적합**. 영업일 데이터 누적 후 `SCREENING_CHANGE_RATE_MAX` 등으로 급등주 편입을 줄여 진입가능 후보를 확보하는 방향을 가설로 제시(자동 구현 금지, 실전 실자금 사안).
3. **[제안서 미생성 — 근본원인 조사 필요]** 실전 `event_logs` 06-02 07:18 이후 미기록. 코드 레벨 조사(event_logger의 real DB 세션/핸들러)가 선행되어야 하므로 무근거 자동 구현 금지. P1로 사람 확인 권고.

## 룰 엔진 판정 요약
| 룰 | 조건 | 판정 |
|----|------|------|
| A 시그널 전환율 저하 | act_rate<50% **AND** avg_conf<0.3 | 미발동 (avg_conf 0.59) |
| B 스크리닝 전환율 악화 | 3일 연속 <10% | 미발동 (06-02만 9.6%) |
| C 에러 반복 | 동일 category 3+ & 전일 동일 | 미발동 (오늘 ERROR 0, event_logs 자체 공백) |
| D API 한도 도달 | api_limit_hits>0 | 미발동 (0) |
| E 리스크 파라미터 | sell_days≥20 | 미발동 (sell_days=0) |

→ 룰 엔진상 자동 제안 대상 없음. 단, **휴장일 게이팅 결함**은 룰 외 사실 기반 버그라 별도 제안서(개선 포인트 1) 1건 생성.
