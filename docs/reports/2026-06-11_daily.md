# [2026-06-11] 일일 매매 리포트

> ⚠️ **분석 대상 DB = `kis_trader_real`** (실전 모드). DB 검증 가드 통과(`current_database()=kis_trader_real`). 모의 `kis_trader`는 분석 대상 아님.
> 🔎 **6/10 대비 변화**: 어제 `signals` 테이블을 채웠던 **대원강업(000430) 미보유 SELL 노이즈(2,252건)가 오늘은 소멸**했다. 대원강업이 오늘 스크리닝 후보에서 빠지면서, 오늘 `signals` 테이블은 **0건**(전 종목 HOLD → 미적재)이고 `SIGNAL_SUMMARY`의 일중 `max_confidence`도 0.432 → **0.0**으로 떨어졌다. 즉 오늘은 SELL 노이즈가 없는 **순수 "전 종목 HOLD" 매매 0건일**이다.

## 요약
| 항목 | 값 |
|------|-----|
| 거래일 성격 | 정상 영업일 (목) — 종일 가동 09:00~15:20 |
| 총 매매 | **0건** (매수 0 / 매도 0) |
| 실현 수익률 | N/A (매수·매도 0건) |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 57종목 (converted_to_trade=4 — **후보 승격 의미, 실체결 아님**, 아래 §스크리닝) |
| 시그널(`signals` 테이블) | **0건** (전 종목 HOLD → 미적재. 어제 같은 미보유 SELL 노이즈 없음) |
| 신호 파이프라인(`system_metrics`) | SIGNAL_SUMMARY 2,250 / SIGNAL_SKIP 9,000 / EVAL_TARGETS 2,278 — **정상 가동(전량 HOLD)** |
| 에러(ERROR) | 0건 (`event_logs`·`system_metrics` 모두 0 — **진짜 무에러**) |
| 사이클 시작/완료/스킵 | **2,281 / 2,281 / 0** (가동률 100%) |
| API 한도 도달 | 0건 |
| 버전 | `src/__version__.py` = `pyproject.toml` = **0.9.2** |

**핵심 한 줄**: 시스템은 09:00~15:20 종일 풀가동(2,281/2,281, API·서킷·에러 0)했고 신호 파이프라인도 정상이었다. 그럼에도 매매 0건인 이유는 **① 보유 포지션이 종일 0(EVAL_TARGETS positions=0)이라 매도 자체가 불가, ② 스크리닝이 후보를 09:03 단 1회만 산출(이후 76 사이클 candidate_count=0)했고, ③ 그 4종목이 종일 stale 평가대상으로 잔류했으며, ④ 4종목 전부 매 사이클 HOLD(buy/sell 모두 0, max_confidence 0.0)**였기 때문이다. 6/9·6/10과 동일한 **만성 구조 문제(보유 0 + 발굴 1회성 + stale 잔류 + 모멘텀 스크리닝↔평균회귀/추세 앙상블 미스매치)**이며, 매매 튜닝 로드맵(Phase 0~4, 커밋 `1fbe738`) 영역이다. 다만 어제와 달리 미보유 SELL 노이즈는 사라져 신호 메트릭 오염은 없었다.

## 일중 거동 (요약)
- 09:00:00~15:20:00 ~10초 간격 2,281 사이클 풀가동. API 한도·서킷 트립·metric ERROR 0건.
- EVAL_TARGETS 분포: **positions=0 / screening=4 / watchlist=0** (2,250 사이클 동일), 초반 28 사이클은 0/0/0(후보 산출 전). 즉 종일 보유 0, 평가대상은 스크리닝 4종목 고정.
- 평가대상 4종목(고정): 케이뱅크(279570)·흥아해운(003280)·랩지노믹스(084650)·SK네트웍스(001740).
  - 6/10 평가대상(흥아해운·GS글로벌·랩지노믹스·후성·대원강업) 대비 흥아해운·랩지노믹스만 유지, 케이뱅크·SK네트웍스 신규 진입, 대원강업·GS글로벌·후성 이탈.

## 체결 내역
당일 체결 **0건**.

> 보유 정본은 KIS 잔고 API 기준(DB `portfolios`는 후행 사본). EVAL_TARGETS의 positions=0이 종일 유지된 점으로 보아 현재 보유 포지션은 비어 있다. 보유 0 → 매도 신호가 나와도 매도 자체가 불가하나, 오늘은 SELL 신호 자체가 0건이었다.

## 전략 시그널 분석
- **`signals` 테이블 오늘 0건.** 어제 테이블을 채웠던 대원강업 미보유 SELL이 오늘은 없다(대원강업이 후보에서 이탈). HOLD는 `_record_signal_to_db`에 적재되지 않으므로, 전 종목 HOLD인 오늘은 `signals`가 비는 것이 정상이다(적재 회귀 아님).
- `system_metrics` 기준 신호 파이프라인은 종일 정상 가동(전량 HOLD):
  - SIGNAL_SUMMARY 2,250건: 매 사이클 `evaluated=4, hold_count=4, buy_count=0, sell_count=0, screening_buy=0, max_confidence=0.0`.
  - SIGNAL_SKIP 9,000건: HOLD 4종목 × 2,250 사이클 = 9,000 (전량 `hold_action` skip).
  - 일중 집계: `sum_buy=0`, `sum_sell=0`, `sum_hold=9,000`, **`day_max_confidence=0.0`**.
- ⚠️ **buy_count 종일 0 · max_confidence 0.0**: 매수 신호가 단 한 건도 없었고, 앙상블 신뢰도가 종일 0.0이었다. 스크리닝 4종목 전부 HOLD. 신규 진입 동력 부재가 매매 0건의 직접 원인이며, 6/9·6/10의 "전 종목 HOLD"와 본질적으로 같다(모멘텀 스크리닝 ↔ 평균회귀/추세 앙상블 미스매치).
- ✅ **미보유 SELL 적재 노이즈 소멸(6/10 §개선포인트 1)**: 6/10 리포트가 지적한 "미보유 종목 SELL 신호 2,252건 적재 오염"은 오늘 발생하지 않았다(노이즈 종목 대원강업이 후보에서 이탈). 단, 이는 근본 수정이 아니라 종목 구성 변화에 따른 자연 소멸이므로, 미보유 후보에 SELL을 산출/적재하는 거동 자체는 재발 가능(6/10 §개선포인트 1 조사 항목 유지).
- 7일 롤링(쿼리 10): ENSEMBLE 2,252건 / act_rate 0.0% / avg_confidence 0.432 — **전량 6/10 단일일 데이터**(오늘 포함 그 외 일자 적재 0). 이 수치로 추세를 판단하지 않으며, 룰 A 판정에도 "의미 있는 매수 기회"로 해석하지 않는다.

## 스크리닝 효율
- 발굴 **57종목**, `converted_to_trade=true` **4종목**(케이뱅크 279570·흥아해운 003280·랩지노믹스 084650·SK네트웍스 001740) → 표기 전환율 7.0%.
- 📌 **converted_to_trade ↔ trades "모순" 해명(6/9·6/10과 동일)**: 오늘 실체결 0인데 converted=4인 것은 데이터 버그가 아니다. converted 4종목이 EVAL_TARGETS 종일 평가대상 4종목과 **정확히 일치**한다. 즉 `converted_to_trade`는 **스크리닝→평가후보 승격**을 의미하며 실체결과 무관하다(전환율 = 체결률이 아니라 후보 승격률).
- ⚠️ **발굴 1회성 + stale 잔류(만성 패턴 지속)**: SCREENING_CANDIDATE 분포 = `candidate_count=4`가 **1 사이클**(09:03:26, cycle 156, ranked_total=30), **`candidate_count=0`이 76 사이클**. 개장 직후 1회만 후보를 산출하고 이후 종일 신규 후보 0이며, 그 4종목이 EVAL_TARGETS에 종일 고정(`_screened_codes` stale 잔류). 6/9·6/10과 동일한 알려진 만성 패턴.
- SCREENING_RISK_EXCLUDED: 77건 — 스크리닝 사이클마다 리스크 게이트 제외 정상 작동.
- 최근 전환율(쿼리 11): 6/5 12.5% / 6/8 6.8% / 6/9 9.5% / 6/10 10.9% / 6/11 7.0% → **3일 연속 <10% 아님**(6/10=10.9%) → 룰 B 미발동. (지표 자체가 체결률이 아니므로 판정 근거 신뢰도 낮음.)

## 에러/경고 · 관측성
| 항목 | 값 | 메모 |
|------|-----|------|
| `event_logs` 오늘 ERROR | **0건** | v0.9.2(6/9) 적재 정합 픽스 반영, 오늘은 진짜 무에러 |
| `system_metrics` ERROR(오늘) | 0건 | metric_errors 0 |
| API_LIMIT / RESTART | 0 / 0 | 가동 안정 |
| 전일(6/10) ERROR | 0건 | 룰 C 비교용 — 연속 에러 없음 |

- ✅ **관측성**: `event_logs` ERROR 0행은 v0.9.2(`50bba8f`, "종목 처리 ERROR를 event_logs에도 적재", 6/9 implemented) 이후 "적재 누락"이 아니라 **실제로 에러가 0건**이었음을 의미한다(`system_metrics` metric_errors=0과 정합).

## 시스템 안정성
- 사이클 2,281 시작 / 2,281 완료(가동률 100%), API 한도·서킷 트립·metric ERROR 0건 → 순수 가동 안정성 양호.
- 가동 윈도우 09:00:00~15:20:00(정상 장 시간). 버전 0.9.2(`__version__.py` = `pyproject.toml` 정합).

## 이전 제안서 효과 검증
- 최근 구현(implementation_logs, 14일):
  - **6/9: `event_logs` 적재 정합 — 종목 처리 ERROR를 event_logs에도 기록**(v0.9.2) → 오늘 `event_logs` ERROR 0행이 "공백"이 아닌 "무에러"임을 보장. **효과 정상.**
  - 6/8: **장 마감 매매 진단 알림(C)** — 오늘처럼 매매 0건일 때 게이트 차단/HOLD 사유를 운영자에게 알리는 채널. 본 리포트의 buy_count=0 / 전 종목 HOLD 진단과 정합.
  - 6/8: **harness git_clean — docs 산출물 untracked 제외**(v0.9.1) → 본 리포트 산출이 git_clean을 막지 않음(효과 정상).
  - 6/3: **holidays.json에 2026-06-03 임시공휴일 추가** — 오늘(영업일)은 직접 검증 대상 아님.

## 개선 포인트
1. **[구조적 · 매매 튜닝 로드맵에서 추적 중 — 제안서 미생성]** 매매 0건의 근본 원인은 **① 보유 0(매도 불가) + ② 스크리닝 후보 1회성 산출(09:03 외 76 사이클 candidate_count=0) + ③ stale 종목 종일 잔류(`_screened_codes`) + ④ 모멘텀 스크리닝 ↔ 평균회귀/추세 앙상블 미스매치(buy_count 종일 0, max_confidence 0.0)**의 결합이다. 이는 스크리닝·후보관리·전략 매칭 등 **실자금 매매 코어 로직 변경**을 요하며 BRIDGE_SPEC 파라미터 자동 튜닝 안전범위를 벗어난다. **매매 튜닝 로드맵 Phase 0~4**(커밋 `1fbe738`)가 이 영역을 계획하므로 **중복 제안서를 생성하지 않는다.** 우선 조사 항목(6/9·6/10과 동일):
   - 스크리닝 후보가 개장 직후 1회만 채워지는 원인(스크리닝 주기 vs `_screened_codes` 갱신/만료 로직).
   - 급등주 위주 스크리닝과 평균회귀/추세 전략의 진입조건 불일치 완화(max_confidence가 종일 0.0으로 진입 임계 근처도 못 가는 점).
2. **[관측 노이즈 · 재발 가능 — 6/10 §개선포인트 1 유지]** 오늘 미보유 SELL 적재 노이즈는 없었으나, 이는 노이즈 종목(대원강업) 이탈에 따른 자연 소멸일 뿐 근본 수정이 아니다. 미보유 스크리닝 후보에 SELL이 산출되고 `sell_without_position`으로 skip되면서도 `signals`에 적재되는 거동은 재발 가능. 수정 대상이 `_record_signal_to_db`(engine.py)/앙상블 산출 로직으로 **실자금 매매 코어 인접**이므로 자동 제안서 미생성, 사람 검토/로드맵 항목으로 유지.
3. **[관측성 · 해소 확인]** `event_logs` ERROR 공백은 v0.9.2로 수정 완료. 오늘 0건은 무에러로 확정. 추가 제안 불요.

## 룰 엔진 판정 요약
| 룰 | 조건 | 판정 |
|----|------|------|
| A 시그널 전환율 저하 | act_rate<50% **AND** avg_conf<0.3 | **미발동** (7일 act_rate 0%지만 avg_conf 0.432 ≥ 0.3; 게다가 2,252건은 6/10 미보유 SELL 단일일 데이터로 매수 기회 의미 없음. 오늘 signals 0건) |
| B 스크리닝 전환율 악화 | 3일 연속 <10% | **미발동** (6/10=10.9%로 연속 깨짐; 지표 자체가 체결률 아님) |
| C 에러 반복 | 동일 category 3+ & 전일 동일 | **미발동** (오늘 ERROR 0, 전일 ERROR 0) |
| D API 한도 도달 | api_limit_hits>0 | **미발동** (0) |
| E 리스크 파라미터 | sell_days≥20 | **미발동** (28일 sell_days=1) |

→ **정량 룰 A~E 전부 미발동 → 자동 제안서 0건.** 룰이 포착하지 못하는 구조적 매매 0건 원인(보유 0 + 발굴 1회성 + HOLD 미스매치)은 §개선포인트에 기록(실자금 코어 변경·로드맵 추적 영역 → 자동 구현 금지, 조사/로드맵 우선).
