# [2026-06-01] 일일 매매 리포트

> ⚠️ **실전(real) 첫 거래일**. 분석 대상 DB = `kis_trader_real` (모의 `kis_trader`는 당일 사이클 0 = 비가동).
> 오늘은 정상 매매일이 아니라 **장중 사이클 폭주 → API 한도 소진 → 자동 중단 → v0.8.3 핫픽스 재시작**으로 이어진 날이다.

## 요약
| 항목 | 값 |
|------|-----|
| 총 매매 | **0건** (매수 0 / 매도 0) |
| 수익률 | N/A (매수 0건) |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 16종목 (전환율 표기 31.3% — 단 실거래 0건이라 플래그 신뢰 불가, 아래 주석) |
| 시그널 | ENSEMBLE 152건 / 실행 0건 (act_rate 0%, avg_conf 0.398) |
| 에러(ERROR) | 0건 (WARNING 1건 = API 일일 한도) |
| 사이클 시작/완료/스킵 | 8701 / 8701 / 0 |
| API 한도 도달 | 1건 (13:53:03, cycle #8520) |

**핵심 한 줄**: 시스템은 종일 가동(사이클 8701)했으나 **오전 사이클 폭주(~2초/사이클)로 13:53에 일일 API 한도(5만)를 소진하고 매매를 자동 중단**했고, **14:50 v0.8.3 재시작 후 ~35분간만 정상 사이클(~10초)** 로 돌았다. 그 짧은 창에서 발생한 유일한 시그널은 삼성전자 **SELL(보유 없음 → 스킵)** 뿐 → **실거래 0건**.

## 일중 타임라인 (시스템 거동)
| 시각(KST) | 사건 | 근거 |
|-----------|------|------|
| 09:00 | 첫 사이클 시작 | system_metrics MIN(CYCLE_START) |
| 09:00~13:53 | **폭주 구간** — 시간당 ~1500~1864 사이클(≈2초/사이클), 누적 8520. 스크리닝 API(get_volume_rank) EGW00201 간헐 거부·재시도 소진으로 시그널 미생성 | 시간대별 CYCLE_START 분포 / v0.8.3 CHANGELOG |
| 13:53:03 | **일일 API 한도 초과 → 당일 매매 사이클 중단** (WARNING) | event_logs `"API 일일 한도 초과, 당일 매매 사이클 중단"`, API_LIMIT detail `{"cycle":8520}` |
| 14:49:58 | **시스템 재시작 (real)** — v0.8.3 핫픽스 반영(운영자 재시작) | event_logs INFO `"시스템 시작 (real)"` |
| 14:54~15:25 | 정상 사이클(~10초 간격). 삼성전자 SELL 시그널 152건 생성, 전건 스킵 | signals 테이블 / 시간당 14h=60·15h=121 |
| 15:25:01 | 마지막 사이클 | system_metrics MAX(CYCLE_START) |

## 체결 내역
체결 **0건**. (오늘 trades 테이블에 BUY/SELL 모두 없음 — 정본 기준 실거래 없음)

## 전략 시그널 분석
- **signal_type 분포**: ENSEMBLE 152건 (전부 삼성전자 005930, 단일 종목 집중).
- **action_taken**: 0건 (act_rate 0%).
- **confidence**: 전건 동일값 **0.398** (min=max=avg). 현재 `STRATEGY_MIN_CONFIDENCE=0.20`(config_overrides) 임계는 **통과**하므로 신뢰도가 미실행 사유가 아님.
- **미실행 사유(signal_value.skip_reason)**: 전건 `"sell_without_position"` — 앙상블이 삼성전자 **SELL**(가중투표 SELL n_win=2, W=0.80)을 권고했으나 **보유 포지션이 없어 매도 불가 → 정당 스킵**.
- **BUY 시그널 0건**: 오늘 저장된 매수 신호 없음. (정상 사이클 가동 시간이 ~35분뿐이었고, 그 사이 삼성전자만 임계 이상 신호를 냄. 나머지 15개 스크리닝 종목은 HOLD/저신뢰로 미저장 — signals 테이블 생존편향)
- **해석**: 0 실행은 **장애가 아니라 게이트 정상 동작**. 빈 포트폴리오(실전 첫날)에서 SELL 신호는 실행 불가하고, 정상 가동 창이 짧아 진입(BUY) 기회가 형성되지 않았다.

## 스크리닝 효율
- 발굴 16종목. `converted_to_trade=true` 5종목(LG디스플레이·SK증권·RISE채권혼합50·휴림로봇·나우IB) → 표기 전환율 31.3%.
- ⚠️ **데이터 정합성 경고**: 실거래는 0건이고 시그널을 낸 삼성전자는 `converted=false`인데, **거래·시그널이 전혀 없는 5종목이 converted=true**. 즉 `converted_to_trade` 플래그가 trades(정본)·signals와 불일치 → **현재 전환율 수치는 신뢰 불가**. (오전 폭주 구간에서 잘못 세팅됐을 가능성. 추적 필요)

## 에러/경고
| 시각 | 심각도 | 내용 | 추정 원인 |
|------|--------|------|----------|
| 13:53:03 | WARNING | API 일일 한도 초과, 당일 매매 사이클 중단 | 오전 사이클 폭주(~175 calls/분)로 일일 5만 호출 소진 (v0.8.3가 해소한 0종목 폴백 버그) |
| 14:49:58 | INFO | 시스템 시작 (real) | v0.8.3 핫픽스 반영 운영자 재시작 |

- `event_logs` ERROR 레벨 **0건**. (EGW00201/스크리너 재시도 소진은 오전 폭주 중 발생했으나 ERROR 이벤트로는 미적재 — 로그 파일 측에 traceback 존재 가능, CHANGELOG v0.8.3 배경 참조)

## 시스템 안정성
- **사이클**: 8701 시작 / 8701 완료 (가동률 100%, 서킷 트립·미완료 0). 단, 8701 중 ~8520은 폭주 구간 산물로 **유효 매매 사이클이 아님**.
- **API 한도**: 13:53 일일 한도 소진 1회. v0.8.3 영향 추정치 = 일일 API ~57k→~5.7k.
- **버전 불일치(부수 관찰)**: CHANGELOG·git HEAD 커밋은 **v0.8.3**이나 `src/__version__.py`=**0.8.2** (bump 누락). 일일 결산 Telegram 헤더 `[v0.8.x]` 표기·롤백 태그 식별에 혼선 소지 → 다음 구현 사이클에서 정합화 권장.

## 이전 제안서 효과 검증
- 최근 14일 `implementation_logs` **0건** (실전 DB가 오늘 부트스트랩 → 이력 없음). 검증 대상 없음.
- 단, 오늘 적용된 **v0.8.3**(CHANGELOG 기준)의 효과는 내일(첫 종일 가동)에 사이클 간격·API 호출량으로 검증 예정.

## 개선 포인트
1. **[룰 D 충족 / 제안서 보류]** `api_limit_hits=1 > 0`로 룰 D 조건은 충족하나, **근본원인(0종목 폴백이 간격 하한을 우회한 사이클 폭주)이 오늘 v0.8.3로 이미 해소**됨. 룰 D의 표준 처방(`SCREENING_INTERVAL_CYCLES`↑/`SCREENING_TOP_N`↓)은 **원인과 무관(오진)** 하므로 제안서를 생성하지 않는다. → **내일(2026-06-02, v0.8.3 첫 종일) API 한도 재도달 시** 제안서 작성(이 경우 _common_rules 의 "2일 연속" 기준도 충족). 추가 여유가 필요하면 `TRADING_MIN_INTERVAL_SECONDS` 상향이 정확한 레버(스크리닝 파라미터 아님).
2. **[관측/추적]** `screening_results.converted_to_trade` 플래그가 trades·signals와 불일치(5 converted vs 0 trade). 전환율 지표 신뢰성 회복을 위해 플래그 세팅 시점 로직 점검 필요. 데이터 더 쌓인 뒤 패턴 확인하고 제안 여부 판단.
3. **[데이터 축적 필요]** 매도일수 0/28일, 정상 가동 시간 ~35분 → 시그널·리스크 파라미터(룰 A·E) 판단 불가. **v0.8.3 하에서 최소 5~20 거래일 축적 후 재분석**.

---
*생성: 일간 루틴 (자동) · 분석 DB: kis_trader_real · 기준시각: 2026-06-01 16:30 KST*
