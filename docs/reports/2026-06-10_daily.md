# [2026-06-10] 일일 매매 리포트

> ⚠️ **분석 대상 DB = `kis_trader_real`** (실전 모드). DB 검증 가드 통과(`current_database()=kis_trader_real`). 모의 `kis_trader`는 분석 대상 아님.
> 🔎 **6/9 대비 변화 정정**: 오늘 `signals` 테이블이 0건(6/9)→**2,252건**으로 채워졌으나, 이는 신호 적재 회복이 아니다. 전량 **대원강업(000430) 단일 종목의 SELL 신호**이며 `skip_reason="sell_without_position"`(보유 0이라 매도 불가)이다. SELL 신호는 HOLD와 달리 `_record_signal_to_db`에 적재되므로 쌓였을 뿐, **실행은 0**이다. `avg_confidence 0.432`는 이 단일 반복 SELL 신호값으로, 실행 가능한 매수 기회를 의미하지 않는다.

## 요약
| 항목 | 값 |
|------|-----|
| 거래일 성격 | 정상 영업일 (수) — 종일 가동 09:00~15:20 |
| 총 매매 | **0건** (매수 0 / 매도 0) |
| 실현 수익률 | N/A (매수·매도 0건) |
| 승률 | N/A (매도 0건) |
| 스크리닝 발굴 | 46종목 (converted_to_trade=5 — **후보 승격 의미, 실체결 아님**, 아래 §스크리닝) |
| 시그널(`signals` 테이블) | **2,252건** (전량 대원강업 SELL·미보유 skip — 실행 0, avg_conf 0.432) |
| 신호 파이프라인(`system_metrics`) | SIGNAL_SUMMARY 2,252 / SIGNAL_SKIP 9,008 / EVAL_TARGETS 2,281 — **정상 가동** |
| 에러(ERROR) | 0건 (`event_logs`·`system_metrics` 모두 0 — **진짜 무에러**, v0.9.2 적재 정합 픽스 반영) |
| 사이클 시작/완료/스킵 | **2,281 / 2,281 / 0** (가동률 100%) |
| API 한도 도달 | 0건 |
| 버전 | `src/__version__.py` = `pyproject.toml` = **0.9.2** |

**핵심 한 줄**: 시스템은 09:00~15:20 종일 풀가동(2,281/2,281, API·서킷·에러 0)했고 신호 파이프라인도 정상이었다. 그럼에도 매매 0건인 이유는 **① 보유 포지션이 종일 0(EVAL_TARGETS positions=0)이라 매도 자체가 불가, ② 스크리닝이 후보를 09시대 단 1회만 산출(이후 77 사이클 candidate_count=0)했고, ③ 그 5종목이 종일 stale 평가대상으로 잔류했으며, ④ 그중 4종목은 매 사이클 HOLD·1종목(대원강업)은 미보유 SELL이라 buy_count가 종일 0**이었기 때문이다. 이는 신호 적재 회귀가 아니라 6/9와 동일한 **만성 구조 문제(보유 0 + 발굴 1회성 + stale 잔류 + 모멘텀 스크리닝↔평균회귀/추세 앙상블 미스매치)**이며, 매매 튜닝 로드맵(Phase 0~4, 커밋 `1fbe738`) 영역이다.

## 일중 거동 (요약)
- 09:00~15:20 ~10초 간격 2,281 사이클 풀가동. API 한도·서킷 트립·metric ERROR 0건.
- EVAL_TARGETS 분포: **positions=0 / screening=5 / watchlist=0** (종일 동일 5종목 고정). 즉 종일 보유 0, 평가대상은 스크리닝 5종목 고정.
- 평가대상 5종목(고정): 흥아해운(003280)·GS글로벌(001250)·랩지노믹스(084650)·후성(093370)·대원강업(000430).

## 체결 내역
당일 체결 **0건**.

> 보유 정본은 KIS 잔고 API 기준(DB `portfolios`는 후행 사본). EVAL_TARGETS의 positions=0이 종일 유지된 점으로 보아 현재 보유 포지션은 비어 있다. 보유 0 → 매도 신호가 나와도(대원강업) 매도 자체가 불가하다.

## 전략 시그널 분석
- **`signals` 테이블 오늘 2,252건 — 전량 대원강업(000430) SELL.** 종목별 분포 쿼리상 단일 종목 2,252행, action_taken 전량 false, avg/max confidence 모두 0.432.
  - signal_value 예: `"앙상블 가중투표: SELL n_win=2 W=0.86 L=0.00 (base=0.43×opp=1.00)"`, `skip_reason="sell_without_position"`, `target_price=null`.
  - 즉 앙상블이 대원강업에 SELL을 산출했으나 **보유분이 0이라 매도할 수 없어 매 사이클 skip**됐다. HOLD와 달리 SELL은 적재되므로 `signals`가 비지 않았을 뿐, **매도 체결로 이어진 신호는 0**이다.
- `system_metrics` 기준 신호 파이프라인은 종일 정상:
  - SIGNAL_SUMMARY 2,252건: 매 사이클 `evaluated=5, hold_count=4, sell_count=1, buy_count=0, screening_buy=0, max_confidence=0.432`.
  - SIGNAL_SKIP 9,008건: HOLD 4종목 × 2,252 사이클 ≈ 9,008 (전량 `skip_reason=hold_action`).
  - 일중 집계: `sum_buy=0`, `sum_sell=2,252`(전량 미보유 skip), `day_max_confidence=0.432`.
- ⚠️ **buy_count 종일 0**: 매수 신호가 단 한 건도 발생하지 않았다. 스크리닝 5종목 중 4종목 HOLD, 1종목(대원강업)은 SELL. 신규 진입 동력 부재가 매매 0건의 직접 원인이며, 6/9의 "전 종목 HOLD" 패턴과 본질적으로 같다(미스매치).
- 🆕 **미보유 종목 SELL 신호 반복 적재(관측 노이즈)**: 스크리닝 후보(매수 대상이어야 할 종목)인 대원강업에 매 사이클 SELL이 산출되고, 미보유라 skip되면서도 `signals`에 2,252건 적재됐다. 이는 ① `signals` 테이블/시그널 메트릭을 오염(avg_confidence가 N/A가 아닌 0.432로 읽힘 → 룰 A 판정에도 영향)시키고, ② 동일 신호의 무의미한 반복 기록이다. **신호 적재 회귀가 아니라, 미보유 종목에 대한 SELL 신호를 적재 전에 거르지 않는 기록 로직의 부작용**이다(아래 §개선포인트 1).
- 7일 롤링(쿼리 10): ENSEMBLE 2,252건 / act_rate 0.0% / avg_confidence 0.432 — **전량 오늘(6/10) 단일일 데이터**(6/4~6/9 적재 0). 이 수치로 추세를 판단하지 않는다.

## 스크리닝 효율
- 발굴 **46종목**, `converted_to_trade=true` **5종목**(흥아해운 003280·GS글로벌 001250·랩지노믹스 084650·후성 093370·대원강업 000430) → 표기 전환율 10.9%.
- 📌 **converted_to_trade ↔ trades "모순" 해명(6/9와 동일)**: 오늘 실체결 0인데 converted=5인 것은 데이터 버그가 아니다. converted 5종목이 EVAL_TARGETS 종일 평가대상 5종목과 **정확히 일치**한다. 즉 `converted_to_trade`는 **스크리닝→평가후보 승격**을 의미하며 실체결과 무관하다(전환율 = 체결률이 아니라 후보 승격률).
- ⚠️ **발굴 1회성 + stale 잔류(만성 패턴 지속)**: SCREENING_CANDIDATE 분포 = `candidate_count=5`가 **1 사이클**(09시대), **`candidate_count=0`이 77 사이클**. 개장 직후 1회만 후보를 산출하고 이후 종일 신규 후보 0이며, 그 5종목이 EVAL_TARGETS에 종일 고정(`_screened_codes` stale 잔류). 6/9와 동일한 알려진 만성 패턴.
- SCREENING_RISK_EXCLUDED: 70건 — 스크리닝 사이클마다 리스크 게이트 제외 정상 작동.
- 최근 전환율(쿼리 11): 6/5 12.5% / 6/8 6.8% / 6/9 9.5% / 6/10 10.9% → **3일 연속 <10% 아님**(6/10=10.9%) → 룰 B 미발동. (7일 이동평균 9.9%로 경계선이나, 지표 자체가 체결률이 아니므로 판정 근거 신뢰도 낮음.)

## 에러/경고 · 관측성
| 항목 | 값 | 메모 |
|------|-----|------|
| `event_logs` 오늘 ERROR | **0건** | v0.9.2 종목 처리 ERROR→event_logs 적재 정합 픽스(6/9) 반영, 오늘은 진짜 무에러 |
| `system_metrics` ERROR(오늘) | 0건 | metric_errors 0 |
| API_LIMIT / RESTART | 0 / 0 | 가동 안정 |
| 전일(6/9) ERROR | 0건 | 룰 C 비교용 — 연속 에러 없음 |

- ✅ **관측성 공백 일부 해소**: 6/9 리포트가 지적한 `event_logs` ERROR 미적재는 v0.9.2(`50bba8f`, "종목 처리 ERROR를 event_logs에도 적재", 6/9 implemented)로 **근본원인이 수정**됐다. 오늘 `event_logs` ERROR 0행은 더 이상 "적재 누락"이 아니라 **실제로 에러가 0건**이었음을 의미한다(`system_metrics` metric_errors=0과 정합). 단, INFO/WARNING 레벨의 실전 적재 여부는 별도 확인 대상으로 남는다.

## 시스템 안정성
- 사이클 2,281 시작 / 2,281 완료(가동률 100%), API 한도·서킷 트립·metric ERROR 0건 → 순수 가동 안정성 양호.
- 가동 윈도우 09:00~15:20(정상 장 시간). 버전 0.9.2(`__version__.py` = `pyproject.toml` 정합).

## 이전 제안서 효과 검증
- 최근 구현(implementation_logs, 14일):
  - **6/9: `event_logs` 적재 정합 — 종목 처리 ERROR를 event_logs에도 기록**(v0.9.2) → 오늘 `event_logs` ERROR 0행이 "공백"이 아닌 "무에러"임을 보장. **효과 검증: 6/6 제안서 `event-logs-error-integrity` 영역 해소.**
  - 6/8: **장 마감 매매 진단 알림(C)** — 오늘처럼 매매 0건일 때 게이트 차단 사유를 운영자에게 알리는 채널. 본 리포트의 buy_count=0 / sell_without_position 진단과 정합.
  - 6/8: **harness git_clean — docs 산출물 untracked 제외**(v0.9.1) → 본 리포트 산출이 git_clean을 막지 않음(효과 정상).
  - 6/3: **holidays.json에 2026-06-03 임시공휴일 추가** — 오늘(영업일)은 직접 검증 대상 아님.

## 개선 포인트
1. **🆕 [관측 노이즈 · 경량 · 단 코어 인접 — 자동 구현 보류]** 미보유 스크리닝 후보(대원강업)에 매 사이클 SELL 신호가 산출·적재되어 `signals` 2,252건이 단일 종목 미보유 SELL로 오염됐다. 이는 신호 메트릭(avg_confidence·act_rate)을 왜곡(룰 A가 읽는 7일 avg_conf가 N/A 대신 0.432가 됨)하고, 동일 신호를 무의미하게 반복 기록한다. 다만 수정 대상은 `_record_signal_to_db`(engine.py) 또는 앙상블의 미보유 종목 SELL 산출 로직이며 **실자금 매매 코어 인접**이므로, 본 루틴에서 자동 제안서로 생성하지 않는다. 다음 분석/사람 검토 항목:
   - `sell_without_position`로 skip되는 SELL 신호는 `action_taken=false`로도 적재할 가치가 있는가(관측 노이즈 vs 진단 가치) — 적재 게이트 검토.
   - 스크리닝 후보(매수 대상)에 SELL이 산출되는 앙상블 거동의 적절성(후보 선정과 신호 방향의 정합).
2. **[구조적 · 매매 튜닝 로드맵에서 추적 중 — 제안서 미생성]** 매매 0건의 근본 원인은 **① 보유 0(매도 불가) + ② 스크리닝 후보 1회성 산출(09시대 외 77 사이클 candidate_count=0) + ③ stale 종목 종일 잔류(`_screened_codes`) + ④ 모멘텀 스크리닝 ↔ 평균회귀/추세 앙상블 미스매치(buy_count 종일 0)**의 결합이다. 이는 스크리닝·후보관리·전략 매칭 등 **실자금 매매 코어 로직 변경**을 요하며 BRIDGE_SPEC 파라미터 자동 튜닝 안전범위를 벗어난다. **매매 튜닝 로드맵 Phase 0~4**(커밋 `1fbe738`)가 이 영역을 계획하므로 **중복 제안서를 생성하지 않는다.** 우선 조사 항목(6/9와 동일):
   - 스크리닝 후보가 개장 직후 1회만 채워지는 원인(스크리닝 주기 vs `_screened_codes` 갱신/만료 로직).
   - 급등주 위주 스크리닝과 평균회귀/추세 전략의 진입조건 불일치 완화.
3. **[관측성 · 해소 확인]** `event_logs` ERROR 공백은 v0.9.2로 수정 완료. 오늘 0건은 무에러로 확정. 추가 제안 불요.

## 룰 엔진 판정 요약
| 룰 | 조건 | 판정 |
|----|------|------|
| A 시그널 전환율 저하 | act_rate<50% **AND** avg_conf<0.3 | **미발동** (act_rate 0%지만 7일 avg_conf 0.432 ≥ 0.3; 게다가 avg_conf는 미보유 SELL 단일값으로 의미 없음) |
| B 스크리닝 전환율 악화 | 3일 연속 <10% | **미발동** (6/10=10.9%로 연속 깨짐; 지표 자체가 체결률 아님) |
| C 에러 반복 | 동일 category 3+ & 전일 동일 | **미발동** (오늘 ERROR 0, 전일 ERROR 0) |
| D API 한도 도달 | api_limit_hits>0 | **미발동** (0) |
| E 리스크 파라미터 | sell_days≥20 | **미발동** (28일 sell_days=1) |

→ **정량 룰 A~E 전부 미발동 → 자동 제안서 0건.** 룰이 포착하지 못하는 구조적 매매 0건 원인(보유 0 + 발굴 1회성 + 미스매치)과 신규 관측 노이즈(미보유 SELL 적재)는 §개선포인트에 기록(실자금 코어 변경·로드맵 추적 영역 → 자동 구현 금지, 조사/로드맵 우선).
